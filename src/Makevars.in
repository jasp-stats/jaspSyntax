PKG_CPPFLAGS = @cppflags@
CXX_STD = CXX20
PKG_LIBS += -L. @libflags@

all: $(SHLIB)
	@if command -v install_name_tool; then install_name_tool -change @rpath/libSyntaxInterface.dylib @loader_path/libSyntaxInterface.dylib $(SHLIB); fi
